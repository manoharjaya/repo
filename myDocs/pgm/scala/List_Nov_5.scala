object List_Nov_5
{
	def main(args: Array[String]): Unit = {
	  

	  var alist:Array[Int]=Array(15,77,487,187)
	  var index=0
	  for(index <- 0 until (alist.length))
	  			println("Index="+alist(index))
		alist=alist+755

	  for(index <- alist)
	  		println(index)




	  		
	}
	
}